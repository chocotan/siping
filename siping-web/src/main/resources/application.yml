server:
  port: 21001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/siping?characterEncoding=UTF-8
    username: root
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
  cache:
    caffeine:
      spec: expireAfterWrite=120m
logging:
  file: ${user.home}/logs/siping.log

